---
import Layout from '../layouts/Layout.astro'
import styles from './index.module.css'
import { links } from './links'
const { email, github, codeberg, telegram, bsky, keyoxide } = links
const selectedLinks = { email, github, codeberg, telegram, bsky, keyoxide }
// Welcome to Astro! Wondering what to do next? Check out the Astro documentation at https://docs.astro.build
// Don't want to use any of this? Delete everything in this file, the `assets`, `components`, and `layouts` directories, and start fresh.
---

<Layout>
  <h1 class={styles.h1}>igor teplostanski</h1>
  <p class='p'>
    Hi everyone 👋 My name is Igor, and I'm a frontend developer. In my free
    time, I develop <a href="https://www.npmjs.com/package/vite-plugin-readable-classnames">vite-plugin-readable-classnames</a>,
    and work on <a href="https://github.com/moonbloom-theme/moonbloom">Moonbloom Theme</a>. I enjoy creating applications and open-source
    projects. Below, I've left all the necessary links for you 👇
  </p>
  <ul class='ul'>
    {
      Object.entries(selectedLinks).map(([_key, { label, link }]) => (
        <li>
          <a href={link} target='_blank' rel='noopener noreferrer'>
            <span>{label}</span>
          </a>
        </li>
      ))
    }
  </ul>
  <footer class='footer'>© 2022-2025</footer>
</Layout>

<style>
  .p::before {
    content: '<p>';
    color: gray;
  }
  .p::after {
    content: '</p>';
    color: gray;
  }
  .ul::before {
    content: '<ul>';
    color: gray;
  }
  .ul::after {
    content: '</ul>';
    color: gray;
  }
  .ul li::before {
    content: '<li>';
    color: gray;
  }
  .ul li::after {
    content: '</li>';
    color: gray;
  }
  .ul {
    list-style: none;
    padding: 0;
  }
  .ul li {
    padding: 0 0 0 1em;
  }
  .footer::after {
    content: '</footer>';
    color: gray;
  }
  .footer::before {
    content: '<footer>';
    color: gray;
  }
</style>
